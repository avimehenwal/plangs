/**
 * FIXME
 * Reason: TypeError: fs.readdirSync is not a function
 */
// var fs = require('fs');
// var files = fs.readdirSync('./');
// console.log(files);

const images = [
  'd3-append.png',
  'devtools-snippets.png',
]

// ES6 module export
export default images;